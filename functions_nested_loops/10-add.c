#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: the added result
 */

int add(int a, int b)
{
	return (a + b);
}
