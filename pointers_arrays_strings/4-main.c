#include "main.h"

/**
 * main - prints a string in reverse way
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Issac Asimov";
	print_rev(str);
	return (0);
}
