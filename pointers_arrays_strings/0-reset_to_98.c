#include "main.h"


/**
 * reset_to_98 - takes a int pointer as parameter & updates points 98
 * @n: a pointer parameter which updates the value it points to 98
 */
void reset_to_98(int *n)
{
	int v = 98;

	*n = v;
}
