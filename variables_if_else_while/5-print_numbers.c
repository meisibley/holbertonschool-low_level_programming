#include <stdio.h>
/**
 * main - prints all sigle digit numbers starting from 0
 *
 * Return: 0 for success
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
